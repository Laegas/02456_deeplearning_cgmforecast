from pathlib import Path
import pathlib

data_path = pathlib.Path().absolute()/'config'/'cgmRaw'
code_path = pathlib.Path().absolute()/'config'/'forecast'
result_path = pathlib.Path().absolute()/'results'
model_path = pathlib.Path().absolute()/'results'/'forecasting'
figure_path = pathlib.Path().absolute()/'results'/'forecasting'
